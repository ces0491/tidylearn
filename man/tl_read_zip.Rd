% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{tl_read_zip}
\alias{tl_read_zip}
\title{Read data from a zip archive}
\usage{
tl_read_zip(path, file = NULL, format = NULL, .quiet = FALSE, ...)
}
\arguments{
\item{path}{Path to a zip file.}

\item{file}{Optional name of a specific file within the archive to read.
Supports partial matching.}

\item{format}{Optional format override for the file(s) inside the archive.}

\item{.quiet}{Suppress messages. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to the format-specific reader.}
}
\value{
A \code{tidylearn_data} object.
}
\description{
Extracts a zip archive to a temporary directory and reads the contents.
If the archive contains a single data file, it is read directly. If
multiple data files are found, they are row-bound with a \code{source_file}
column. Use the \code{file} argument to select a specific file from
the archive.
}
\examples{
\donttest{
# Read from a zip archive
# data <- tl_read_zip("data.zip")

# Read a specific file from the archive
# data <- tl_read_zip("data.zip", file = "train.csv")
}

}
