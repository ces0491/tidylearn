% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud-consent.R
\name{tl_cloud_consent}
\alias{tl_cloud_consent}
\title{Grant or revoke cloud upload consent for this R session}
\usage{
tl_cloud_consent(consent = TRUE)
}
\arguments{
\item{consent}{\code{TRUE} to allow cloud uploads for the rest of the
session, \code{FALSE} to revoke.}
}
\value{
The previous consent state, invisibly.
}
\description{
Fitting with \code{compute = "cloud"} uploads your training data to your
own Modal account, which is a third party. tidylearn will not do that
without explicit consent on every call.
}
\details{
There are two ways to give it. Pass \code{confirm_upload = TRUE} to each
\code{tl_model()} call, or call \code{tl_cloud_consent()} once to opt in for the
rest of the session. The session lock exists for batch and
non-interactive work, where a per-call argument is repetitive.

The lock is \strong{not} persisted. It is forgotten when the R session
ends, and it is never written to disk. Revoke it early with
\code{tl_cloud_consent(FALSE)}.

tidylearn never prompts interactively for consent, so scripts, CI and
\code{Rscript} behave the same as an interactive session.
}
\examples{
# Opt in for the session, then revoke.
old <- tl_cloud_consent(TRUE)
tl_cloud_consent(FALSE)
}
\seealso{
The full contract is in
\code{system.file("security/threat-model.md", package = "tidylearn")}.
}
