% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-advisor.R
\name{tl_compute_advisor}
\alias{tl_compute_advisor}
\alias{tl_compute_advisor.character}
\alias{tl_compute_advisor.tidylearn_supervised}
\alias{tl_compute_advisor.default}
\title{Advise on the best compute tier for a tidylearn fit}
\usage{
tl_compute_advisor(x, ...)

\method{tl_compute_advisor}{character}(
  x,
  data,
  formula = NULL,
  hyperparams = list(),
  gpu_check = NULL,
  ...
)

\method{tl_compute_advisor}{tidylearn_supervised}(
  x,
  data = NULL,
  formula = NULL,
  hyperparams = list(),
  gpu_check = NULL,
  ...
)

\method{tl_compute_advisor}{default}(x, ...)
}
\arguments{
\item{x}{Either a method name (character scalar — same names as
accepted by \code{\link[=tl_model]{tl_model()}}, supervised methods only) or a fitted
\code{tidylearn_supervised} model. When given a fitted model, the
advisor introspects its method and formula and advises on what a
refit (or a fit on similar new data) would cost.}

\item{...}{Unused, reserved for method-specific extensions.}

\item{data}{A data frame. Required when \code{x} is a method name;
optional when \code{x} is a fitted model (defaults to the model's
training data).}

\item{formula}{Optional formula. Used to determine the number of
effective predictors. Ignored when \code{x} is a fitted model.}

\item{hyperparams}{Named list of hyperparameters that affect runtime
(e.g. \code{list(nrounds = 1000)} for xgboost, \code{list(epochs = 50, units = 256)} for deep learning). Missing entries fall back to
per-method defaults.}

\item{gpu_check}{Optional \code{tidylearn_gpu_check} object. If omitted,
\code{tl_check_gpu()} is called once internally.}
}
\value{
An object of class \code{tidylearn_compute_advice} containing
\code{problem}, \code{local_cpu}, \code{local_gpu}, \code{cloud}, \code{recommendation},
and \code{reasoning}. A \code{print()} method is provided.
}
\description{
Estimates runtime and feasibility on local CPU, local GPU (when
available and applicable), and cloud GPU (stubbed until cloud
integration lands), then returns a structured recommendation. Useful
before kicking off a long fit — call this first to see whether the
problem is laptop-sized, GPU-sized, or cloud-sized.
}
\details{
Estimates are deliberately rough — order-of-magnitude, not bills.
Per-method scaling constants are calibrated against typical hardware
and will be off by 2-3x in either direction for any individual job.
Treat the recommendation as a starting point, not gospel.
}
\examples{
# Estimating from a method name needs neither a GPU nor the backend
# package -- it is arithmetic over the problem dimensions
advice <- tl_compute_advisor("xgboost", iris, Species ~ .,
                             hyperparams = list(nrounds = 1000))
advice$recommendation
print(advice)

\donttest{
# Dispatching on a fitted model requires the backend to be installed
if (requireNamespace("xgboost", quietly = TRUE)) {
  model <- tl_model(iris, Species ~ ., method = "xgboost")
  tl_compute_advisor(model)
}
}
}
