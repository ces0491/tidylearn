% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud-endpoint.R
\name{tl_cloud_allow_host}
\alias{tl_cloud_allow_host}
\title{Allow an additional host for cloud uploads in this R session}
\usage{
tl_cloud_allow_host(host)
}
\arguments{
\item{host}{A character vector of host names to allow, or \code{NULL} to
clear every host added this session. Bare host names only — not
URLs, ports, paths or wildcards.}
}
\value{
The full allowlist after the change, invisibly.
}
\description{
tidylearn uploads training data only to Modal's own hosts
(\verb{*.modal.run}, \verb{*.modal.com}). Modal customers serving Web Functions
from a custom domain can add that domain here.
}
\details{
This widens the set of destinations your data may be sent to, so it
is deliberately a per-session call rather than an option or an
environment variable: a shared \code{.Rprofile} or an inherited environment
should not be able to add a destination without you writing the call.
Additions are never persisted and are forgotten when the session ends.

Hosts match themselves and their subdomains. Adding
\code{"fits.example.com"} accepts \verb{https://fits.example.com} and
\verb{https://a.fits.example.com}, and nothing else.
}
\examples{
tl_cloud_allow_host("fits.example.com")
tl_cloud_allowed_hosts()
tl_cloud_allow_host(NULL)
}
\seealso{
\code{\link[=tl_cloud_allowed_hosts]{tl_cloud_allowed_hosts()}}, and T9 in
\code{system.file("security/threat-model.md", package = "tidylearn")}.
}
