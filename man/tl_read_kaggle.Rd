% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-backends.R
\name{tl_read_kaggle}
\alias{tl_read_kaggle}
\title{Read from Kaggle}
\usage{
tl_read_kaggle(source, file = NULL, dest = tempdir(), type = "dataset", ...)
}
\arguments{
\item{source}{A Kaggle dataset slug (e.g., \code{"user/dataset-name"}) or a
Kaggle URL.}

\item{file}{The specific file to read from the dataset. If \code{NULL} and
the dataset contains exactly one file, it is read automatically.}

\item{dest}{Directory to download files to. Default is a temporary directory.}

\item{type}{Either \code{"dataset"} (default) or \code{"competition"}.}

\item{...}{Additional arguments passed to the format-specific reader.}
}
\value{
A \code{tidylearn_data} object containing the downloaded data.
}
\description{
Downloads a dataset file from Kaggle using the Kaggle CLI and reads it into
a \code{tidylearn_data} object. Requires the Kaggle CLI to be installed and
configured (\code{pip install kaggle}).
}
\examples{
\donttest{
# data <- tl_read_kaggle("zillow/zecon", file = "Zip_time_series.csv")
# data <- tl_read_kaggle("titanic", file = "train.csv", type = "competition")
}

}
